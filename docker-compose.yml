version: '3.8'
services:
  db:
    image: postgres:15
    container_name: lims_db
    restart: always
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: lims
    ports:
      - "5432:5432"